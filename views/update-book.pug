extends layout 

block content
    if(errors)
        h2.errors.error Oops an error occurred!
        ul.errors.error
            each error in errors
                li= error.message
    h1= title 
    form(method='POST')
        p 
        label(for='title') Title: 
        input(type='text', name='title', value=book.title)
        p
        label(for='author') Author: 
        input(type='text', name='author', value=book.author)
        p
        label(for='genre') Genre: 
        input(type='text', name='genre', value=book.genre)
        p
        label(for='year') Author: 
        input(type='text', name='year', value=book.year)
        p
        input(type='submit' value='Update Book')
        p
    form(action='/books/' + book.id + '/delete', method='POST' onsubmit="return confirm ('Do you really wnat to delete this book?')")
        p 
            a(class='button' href='../books') Cancel
        input(type='submit' value='Delete Book')
        